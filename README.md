# lib_numeric

This repository owns two narrow targets: `p101_math` for mathematical
extensions and `p101_random` for random-number interfaces. They remain separate
link libraries so random users do not acquire a `libm` dependency.
